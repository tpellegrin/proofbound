# Moved

The Proofbound architecture is no longer a single document. It was split for progressive disclosure:
a bounded task should be able to read only the architecture that applies to it.

**Start here: [`proofbound/README.md`](proofbound/README.md).**

This file is a redirect. It carries no architectural content and is not authoritative for anything.

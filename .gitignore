__pycache__/
*.pyc

# Runtime artifacts of running/installing the skill against this repo itself
# (.proofbound/ is the per-project run workspace, DeepSeekAndDestroy/ its
# legacy name; .kilo/ is what scripts/install_kilo_agents.py writes) -- none
# belongs in the skill package's own history.
.proofbound/
DeepSeekAndDestroy/
.kilo/

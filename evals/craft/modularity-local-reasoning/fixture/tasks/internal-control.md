# Task — NOT BUILT

The approved boundary-crossing control (*change the storage retry behaviour*) is incompatible with
the approved isolation mechanism: with the package materialised outside the workspace, neither arm
can modify it, so the control would fail in both. See `ground-truth.md` and the implementation
record. No control task is frozen in this revision.
